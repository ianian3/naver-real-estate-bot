# 📥 데이터 가져오기 문제 해결가이드

## 🔴 현재 문제

다운로드한 JSON 파일의 `listings` 배열이 **비어있습니다**.

```json
{
  "metadata": {
    "complex_no": "113432",
    "complex_name": "래미안솔베뉴",
    ...
  },
  "listings": []  // ← 비어있음!
}
```

## 🔍 원인 분석 및 해결 방법

Tampermonkey 스크립트가 네이버 부동산 페이지의 가격 데이터를 수집하지 못했습니다.

### ✅ 단계별 해결 방법

#### 1️⃣ **페이지 완전 로드 확인** (가장 중요!)

1. **네이버 부동산** 접속: https://new.land.naver.com
2. **원하는 단지 클릭** (예: 래미안솔베뉴)
3. **매물 리스트가 완전히 로드될 때까지 대기**
   - 우측 리스트를 **아래로 스크롤**
   - 추가 매물이 계속 로드되는지 확인
   - 로드 완료까지 **30초 이상 대기**

#### 2️⃣ **필터 설정 확인**

좌측 필터에서:
- ✅ **59m²** 면적이 보이는지 확인
- ✅ **84m²** 면적이 보이는지 확인
- ⚠️ 기본값으로 설정 (필터가 너무 좁혀있으면 안됨)

#### 3️⃣ **Tampermonkey 스크립트 활성화 확인**

1. **브라우저 우측 상단** Tampermonkey 아이콘 클릭
2. 현재 사이트에서 **스크립트가 활성화**되었는지 확인
3. 활성화 안 되면 체크박스 클릭

#### 4️⃣ **브라우저 콘솔 확인** (필수)

1. **F12** 키로 개발자 도구 열기
2. **Console** 탭 클릭
3. **빨간색 에러 메시지** 확인
4. 다음 명령어 실행:

```javascript
// 수집된 가격 데이터 확인
const result = getPrice_WeolbuStandard();
console.log('수집된 데이터:', result);
console.log('면적 개수:', Object.keys(result).length);

// 만약 데이터가 있으면 저장
if (Object.keys(result).length > 0) {
    saveToLocalStorage();
}
```

**출력 예시:**
```
수집된 데이터: {
  "59A/59m²": {매매: 120000, 전세: 80000, ...},
  "84A/84m²": {매매: 180000, 전세: 120000, ...}
}
면적 개수: 2
```

#### 5️⃣ **데이터 저장 및 내보내기**

콘솔에서 데이터가 보이면:

```javascript
// 1. 현재 단지 저장
saveToLocalStorage();

// 2. 모든 저장된 단지 내보내기
exportAllData();
```

## 💾 수동 저장 방법

콘솔에서 다음 코드 실행:

```javascript
// LocalStorage에 데이터 직접 저장
const manualData = [{
  metadata: {
    complex_no: "113432",
    complex_name: "래미안솔베뉴",
    address: "서울시 강남구",
    total_households: 1900,
    collected_at: new Date().toISOString(),
    collector: "manual"
  },
  listings: [
    {
      area_type: "59A/59m²",
      exclusive_area: 59,
      sale_price: 120000,
      sale_floor: "고층",
      sale_count: 5,
      lease_price: 80000,
      lease_floor: "고층",
      lease_count: 3
    },
    {
      area_type: "84A/84m²",
      exclusive_area: 84,
      sale_price: 180000,
      sale_floor: "중층",
      sale_count: 8,
      lease_price: 120000,
      lease_floor: "중층",
      lease_count: 4
    }
  ]
}];

localStorage.setItem('naver_real_estate_data', JSON.stringify(manualData));
alert('✅ 데이터가 저장되었습니다.');
```

## 🔧 Tampermonkey 스크립트 재설치

1. **Tampermonkey 대시보시** 열기
2. 기존 스크립트 삭제
3. **new script** 클릭
4. [tampermonkey_export.js](./tampermonkey_export.js) 파일 전체 복사
5. 새로운 스크립트에 붙여넣고 **Save**

## 🆘 계속 문제가 발생하면

### 디버그 정보 수집

콘솔에서:

```javascript
// 1. 페이지 요소 확인
console.log('매물 리스트 요소:', document.querySelector('#articleListArea'));
console.log('페이지 제목:', document.querySelector('#complexTitle')?.innerText);

// 2. 데이터 수집 시도
const priceData = getPrice_WeolbuStandard();
console.log('수집된 데이터:', priceData);

// 3. 콘솔 출력 전체 복사 (우클릭 → Save as)
```

### 정보 공유

문제 해결을 위해 다음을 알려주세요:
- 브라우저 콘솔의 **에러 메시지**
- `getPrice_WeolbuStandard()` **출력값**
- 현재 네이버 부동산 **URL**

---

## 📝 JSON 파일 구조 참고

올바른 JSON 구조:

```json
{
  "metadata": {
    "export_date": "2026-01-16T00:00:00.000Z",
    "total_complexes": 1,
    "complex_names": "래미안솔베뉴"
  },
  "complexes": [
    {
      "metadata": {
        "complex_no": "113432",
        "complex_name": "래미안솔베뉴",
        "address": "서울시 강남구",
        "total_households": 1900,
        "collected_at": "2026-01-16T00:00:00.000Z",
        "collector": "tampermonkey_script"
      },
      "listings": [
        {
          "area_type": "59A/59m²",
          "exclusive_area": 59,
          "sale_price": 120000,     // 만원 단위
          "sale_floor": "고층",
          "sale_count": 5,
          "lease_price": 80000,
          "lease_floor": "고층",
          "lease_count": 3,
          "gap": 40000,
          "lease_rate": "67%"
        }
      ]
    }
  ]
}
```

**💡 팁:** 가격은 **만원 단위**입니다 (1억 = 10000만원)
